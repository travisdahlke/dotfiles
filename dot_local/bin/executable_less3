#!/bin/sh
if ! command -v aws &> /dev/null
then
  echo "aws could not be found: Try: brew install awscli"
  exit
fi

if ! command -v jq &> /dev/null
then
  echo "jq could not be found: Try: brew install jq"
  exit
fi

aws s3 cp $1 - | jq . | less
